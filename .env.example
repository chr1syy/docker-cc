# Example environment variables for Docker CC
ADMIN_USER=admin # default admin username
ADMIN_PASSWORD_HASH= # bcrypt hash of admin password (required)
ALLOW_ACTIONS=false # enable start/stop/restart actions
SESSION_SECRET= # random string for session signing (required)
SESSION_TTL=24h # session inactivity TTL
STATIC_DIR=./static # where built frontend is served from
